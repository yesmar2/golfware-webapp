import styled from 'styled-components';

export default styled.p`
    margin: ${(props) => props.theme.spacing(6)}px 0 ${(props) => props.theme.spacing(3)}px;
    font-size: 18px;
    text-align: center;
`;
